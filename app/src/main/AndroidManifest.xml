<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.habtracker20">

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup"
        android:icon="@drawable/ic_launcher_habtrack_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_habtrack_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.HabTracker20">
        <activity android:name=".viewTasks" />
        <activity android:name=".CreateTask" />
        <activity android:name=".CalendarActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.HabTracker20.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>